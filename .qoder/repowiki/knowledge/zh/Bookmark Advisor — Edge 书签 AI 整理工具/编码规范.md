- 所有跨端数据交换采用可读 JSON 文件（snapshot / plan / reviewed_plan / report / job），作为唯一契约而非内存对象
- AI 调用统一走 OpenAI 兼容 REST，CLI 用 openai SDK，扩展用原生 fetch，均支持自定义 BASE_URL 与模型名
- 变更类操作遵循 per-action review + undo log 模式，任何 mutation 前记录父节点/标题预状态以便回滚
- 受保护根与强制重定位等策略集中定义于 `config/rules.yaml`，扩展与 CLI 共用同一份 guardrails